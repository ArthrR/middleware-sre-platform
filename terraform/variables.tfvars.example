# =============================================================
# Example variable overrides for ai-platform-lab
# Copy to terraform.tfvars and adjust as needed
# Never commit terraform.tfvars to version control!
# =============================================================

kubeconfig_path  = "~/.kube/config"
kube_context     = "docker-desktop"   # or "minikube", "kind-kind"
namespace        = "enterprise-middleware"
environment      = "local"             # local | staging | prod
app_replicas     = 2
enable_monitoring = true
enable_ai_platform = false   # set true to deploy the ai-gateway ConfigMap + Qdrant via Helm
